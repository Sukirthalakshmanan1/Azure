use Meesho
create table customer(custid int primary key,custname varchar(20))
insert into customer values(1,'suki')
insert into customer values(2,'sam')
select * from customer

SELECT CONNECTIONPROPERTY('local_net_address')
select * from sys.dm_exec_connections

